# pytest 설정 — backend/ 에서 `python -m pytest -q` 로 실행
[pytest]
# tests/ 폴더만 테스트 대상으로 찾는다
testpaths = tests
# 외부 라이브러리의 사용 중단(Deprecation) 경고는 결과 화면에서 숨긴다
filterwarnings =
    ignore::DeprecationWarning
